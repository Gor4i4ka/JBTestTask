package stuff;

public class TernaryTest {

    int func(int in) {
        int a;
        a = in > 0 ? 1 : 0;
        String c = "asda";
        String b = "sfs";
        if (c != b)
            return 1;
        return 0;
    }
}
